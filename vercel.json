{
  "framework": "nextjs",
  "buildCommand": "cd startup-simulation && npm install && npm run build",
  "outputDirectory": "startup-simulation/.next",
  "installCommand": "echo 'install via buildCommand'"
}
